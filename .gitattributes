# The seed dump is tracked binary content. Marking it explicitly keeps git from
# attempting text normalisation or a useless diff on the compressed game data.
databases/seed.sql.gz binary

# Shell scripts must stay LF so they run on the Linux VM regardless of the
# committing host's platform.
*.sh text eol=lf
